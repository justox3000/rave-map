<%= render "shared/navbar" %>
<%= render "shared/search_form" %>
<div class="dashboard-container">
  <h1>Your Events</h1>
  <div class="Calendar">
    <%# <%= month_calendar(events: @parties) do |date, parties| %>
      <%# <%= date %>
      <%# <% parties.each do |party| %>
        <%# <%= link_to party_path(party) do %>
          <%# <div class="party"> %>
            <%# <p><%= party.title</p> %>
          <%# </div> %>
        <%# <% end %>
      <%# <% end %>
    <%# <% end %>
    <%= week_calendar events: @parties do |date, parties| %>
      <%= date.to_formatted_s(:short) %>
        <% parties.each do |party| %>
          <%= link_to party_path(party) do %>
            <div class="party">
              <p><%= party.title %></p>
            </div>
          <% end %>
        <% end %>
    <% end %>
  </div>
</div>
<div class="dashboard-container">
  <h2 class="mb-0">Bookmarked </h2>
</div>
<div class="index-container">
  <% @favorites.each do |party| %>
    <%= link_to party_path(party) do %>
        <div class="card-party">
          <%= cl_image_tag party.photo.key, height: 200 %>
          <div class="card-party-infos">
            <h3><%= party.title %></h3>
            <div class="card-details">
              <p><strong>Location: </strong><%= party.location %></p>
              <p><strong>Date: </strong><%= party.date %></p>
            </div>
          </div>
        </div>
      <% end %>
  <% end %>
</div>
